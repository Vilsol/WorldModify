package worldmodify;

import org.bukkit.ChatColor;

public class R {

	public static final String prefix = ChatColor.GOLD + "[WM] " + ChatColor.AQUA;
	public static final String prefixe = ChatColor.GOLD + "[WM] " + ChatColor.DARK_RED;

	public static final String title = ChatColor.GOLD.toString();
	public static final String left = ChatColor.GREEN + "Left" + ChatColor.WHITE + ": ";
	public static final String right = ChatColor.RED + "Right" + ChatColor.WHITE + ": ";
	public static final String leftright = ChatColor.GREEN + "Left" + ChatColor.WHITE + "/" + ChatColor.RED + "Right" + ChatColor.WHITE + ": ";
	
}
